# Оглавление

1. [Что такое Maven? Для чего он нужен? Как добавлять библиотеки без него?](#maven-basics)
2. [Как добавить dependency в Maven? Для чего они нужны? Откуда скачиваются?](#maven-dependencies)
3. [Основные фазы Maven-проекта](#maven-phases)
4. [Что такое JDBC? Основные классы и интерфейсы](#jdbc-basics)
5. [Для чего нужен DriverManager](#jdbc-drivermanager)
6. [Statement, PreparedStatement, CallableStatement](#jdbc-statements)
7. [Что такое SQL-injection](#sql-injection)
8. [Что такое ResultSet и как с ним работать](#jdbc-resultset)
9. [Паттерн DAO](#dao-pattern)
10. [Что такое JPA](#jpa-basics)
11. [Что такое ORM](#orm-basics)
12. [Что такое Hibernate](#hibernate-basics)
13. [Разница между JPA и Hibernate](#jpa-vs-hibernate)
14. [JPA/Hibernate классы и интерфейсы](#jpa-hibernate-interfaces)
15. [Основные аннотации Hibernate](#hibernate-annotations)
16. [Отличия HQL от SQL](#hql-vs-sql)
17. [Что такое Query и как передавать параметры](#hibernate-query)
18. [Параметры hbm2ddl](#hbm2ddl-options)
19. [Требования JPA к Entity-классам](#jpa-entity-requirements)
20. [Жизненный цикл Entity в Hibernate](#hibernate-entity-lifecycle)


<h2 id="maven-basics">1. Что такое Maven? Для чего он нужен? Как добавлять библиотеки без него?</h2>

Что такое Maven? Для чего он нужен? Как добавлять библиотеки без него?

Maven — это инструмент сборки и управления проектами в Java.
Он решает три главные задачи:

Сборка проекта — компиляция, запуск тестов, упаковка .jar/.war.

Управление зависимостями — автоматическое скачивание библиотек.

Стандартизация структуры проекта — у всех Maven-проектов одинаковая структура папок.

🔹 Как добавлять библиотеки без Maven?

Есть два способа:

1. Скачать JAR-файл вручную

Ты скачиваешь библиотеку (например, MySQL Connector) → кладёшь в libs/ → добавляешь в classpath.

2. Указать путь к библиотеке в IDE

Например:
File → Project Structure → Libraries → Add JAR.


<h2 id="maven-dependencies">2. Как добавить dependency в Maven? Для чего они нужны? Откуда скачиваются?</h2>

Как добавить dependency в Maven? Для чего они нужны? Откуда скачиваются?

Dependency — это внешняя библиотека, которую проект использует в работе.
Maven сам скачивает их, подключает и следит за версиями.

Как добавить dependency

В файл pom.xml в раздел <dependencies> добавляют блок вида:

<dependency>
  <groupId>org.hibernate</groupId>
  <artifactId>hibernate-core</artifactId>
  <version>5.6.0.Final</version>
</dependency>


После сохранения Maven автоматически скачает библиотеку.

Для чего нужны dependency

подключают внешние библиотеки в проект

упрощают работу с версиями

позволяют не хранить JAR-файлы вручную

автоматически подтягивают зависимые библиотеки (transitive dependencies)

Откуда dependency скачиваются

Из Maven-репозиториев:

Локальный репозиторий — .m2/repository

Центральный репозиторий Maven Central

Дополнительные репозитории, если указаны в pom.xml

<h2 id="maven-phases">3. Основные фазы Maven-проекта</h2>

Основные фазы Maven-проекта

Maven делит процесс сборки на логические этапы. Самые важные:

clean

Удаляет старые файлы сборки.
То есть очищает папку target, чтобы следующая сборка была “с нуля”.

validate

Проверяет структуру проекта.
Смотрит, что все файлы на месте, и проект можно собирать.

compile

Компилирует исходный код Java.
Результат кладётся в target/classes.

test

Запускает тесты.
Проект ещё не упакован в jar, но весь код компилируется и проверяется.

package

Упаковывает проект в артефакт — обычно .jar или .war.
Файл появится в target/имя-проекта.jar.

verify

Проводит дополнительные проверки качества.
Используется для проектов, где есть валидации после упаковки.

install

Устанавливает собранный jar в локальный репозиторий .m2/repository.
Благодаря этому другие локальные проекты могут использовать его как зависимость.

site

Генерирует документацию по проекту (отчёты, метрики, сайт Maven).
Эта фаза редко используется в учебных проектах.

deploy

Загружает финальный артефакт в удалённый репозиторий (например Nexus).
Применяется в командной разработке, когда сборка должна быть доступна на сервере.

✔ Важное

В реальности чаще всего используются:

clean

compile

test

package

install

Остальные — реже, но они всё равно важны для понимания.

<h2 id="jdbc-basics">4. Что такое JDBC? Какие классы/интерфейсы относятся к JDBC?</h2>

clean

Удаляет старые файлы сборки.
То есть очищает папку target, чтобы следующая сборка была “с нуля”.

validate

Проверяет структуру проекта.
Смотрит, что все файлы на месте, и проект можно собирать.

compile

Компилирует исходный код Java.
Результат кладётся в target/classes.

test

Запускает тесты.
Проект ещё не упакован в jar, но весь код компилируется и проверяется.

package

Упаковывает проект в артефакт — обычно .jar или .war.
Файл появится в target/имя-проекта.jar.

verify

Проводит дополнительные проверки качества.
Используется для проектов, где есть валидации после упаковки.

install

Устанавливает собранный jar в локальный репозиторий .m2/repository.
Благодаря этому другие локальные проекты могут использовать его как зависимость.

site

Генерирует документацию по проекту (отчёты, метрики, сайт Maven).
Эта фаза редко используется в учебных проектах.

deploy

Загружает финальный артефакт в удалённый репозиторий (например Nexus).
Применяется в командной разработке, когда сборка должна быть доступна на сервере.

✔ Важное

В реальности чаще всего используются:

clean

compile

test

package

install

Остальные — реже, но они всё равно важны для понимания.

<h2 id="jdbc-drivermanager">5. Для чего нужен DriverManager?</h2>

DriverManager — это класс, который управляет драйверами JDBC и помогает приложению получить соединение с базой данных.

Основные задачи:

1. Хранит зарегистрированные драйверы
Когда драйвер базы подключается к проекту, он регистрируется в DriverManager.

2. Выбирает нужный драйвер по URL
Например
jdbc:mysql
jdbc:postgresql
jdbc:h2

DriverManager сам определяет, какой драйвер подходит.

3. Создаёт Connection
Через метод
getConnection(url, username, password)
он возвращает объект Connection.

DriverManager — это посредник между приложением и конкретным JDBC-драйвером.

<h2 id="jdbc-statements">6. Что такое Statement, PreparedStatement, CallableStatement?</h2>

Statement
Используется для выполнения простых SQL-запросов без параметров.
Подходит для разовых запросов, но не защищает от SQL-инъекций.

PreparedStatement
Предварительно компилируемый SQL-запрос с параметрами.
Главные преимущества

безопасен от SQL-инъекций

быстрее при повторном использовании

удобная подстановка значений через setInt, setString и другие методы

CallableStatement
Используется для вызова хранимых процедур в базе данных.
Позволяет передавать параметры и получать выходные значения.

<h2 id="sql-injection">7. Что такое SQL-injection?</h2>

SQL-инъекция — это способ атаки, при котором злоумышленник подставляет в запрос свои SQL-команды, чтобы изменить или получить данные из базы.

Инъекция происходит, когда приложение вставляет значения прямо в SQL-строку, например:

SELECT * FROM users WHERE name = '" + userInput + "'


Если пользователь введёт
" OR "" = "
то запрос станет валидным и вернёт все данные.

Почему это опасно

позволяет читать данные

изменять или удалять таблицы

обходить авторизацию

выполнять произвольные SQL-команды

Как защититься

использовать PreparedStatement

никогда не конкатенировать SQL-строки

проверять и экранировать ввод

ограничивать права пользователя БД

<h2 id="jdbc-resultset">8. Что такое ResultSet? Как с ним работать?</h2>

ResultSet — это объект, который содержит результат выполнения SQL-запроса SELECT.
Он работает как "курсор", который перемещается по строкам результата.

Основные операции

1. Перемещение по данным
Метод next() передвигает курсор на следующую строку.
Возвращает true, пока есть строки.

2. Чтение значений
Данные извлекаются по имени или номеру столбца:

getInt
getString
getBoolean
getDouble
и другие методы.

3. Порядок работы

выполнить запрос

получить ResultSet

пройти по данным через next()

прочитать нужные значения

закрыть ResultSet и Statement

Важно помнить

ResultSet нужно закрывать

По умолчанию он только вперёд (только next)

Тип ResultSet можно менять (scroll, update), но это используется реже

ResultSet — это мост между SQL-таблицей и Java-объектами, позволяющий построчно читать данные, возвращенные базой.

<h2 id="dao-pattern">9. Рассказать про паттерн DAO</h2>

DAO — это подход, который отделяет логику работы с базой данных от остальной части приложения.

Основная идея
Код, который читает и пишет данные, вынесен в отдельный слой — Data Access Object.
Остальная программа не знает, как именно происходит доступ к БД. Она работает только с методами DAO.

Зачем нужен DAO

Упрощает код — запросы не смешаны с бизнес-логикой

Улучшает тестирование — можно подменить DAO на фейковую реализацию

Легко менять способ хранения данных — SQL, Hibernate, JSON — всё скрыто внутри DAO

Делает проект структурированным и понятным

Пример ответственности DAO

получить всех пользователей

удалить пользователя

обновить запись

сохранить новую запись

DAO не должен решать бизнес-задачи — только работать с хранилищем данных.

<h2 id="jpa-basics">10. Что такое JPA?</h2>

JPA — это стандарт Java для работы с объектно-реляционным отображением.
Он определяет единый набор правил, по которым Java-объекты связываются с таблицами базы данных.

Важно понимать
JPA — это только спецификация.
Она описывает, как должно работать ORM, но сама по себе не содержит кода.

Чтобы JPA работала в реальном проекте, нужен провайдер — например Hibernate.

Что дает JPA

Унифицированный подход к работе с базой

Аннотации для описания сущностей

Механизм управления объектами и их состояниями

Возможность писать код независимо от конкретной ORM-библиотеки

То есть JPA упрощает работу с БД и делает код переносимым между разными ORM-решениями.

<h2 id="orm-basics">11. Что такое ORM?</h2>

ORM — это способ работать с базой данных не через SQL-запросы, а через обычные Java-объекты.

Идея
Вместо таблиц, строк и столбцов программист работает с классами и объектами.
ORM автоматически преобразует объекты в SQL и обратно.

Что делает ORM

сохраняет объекты в базу

загружает данные и превращает их в Java-объекты

обновляет и удаляет записи

управляет связями между объектами

Зачем нужна ORM

уменьшает количество SQL-кода

делает проект чище и понятнее

ускоряет разработку

снижает количество ошибок

помогает работать с разными базами без переписывания кода

Примеры ORM
Hibernate
EclipseLink
TopLink

ORM — это мост между объектным миром Java и реляционными таблицами базы данных.

JPA — Java Persistence API
Стандарт Java для работы с данными в базе через объекты.

ORM — Object Relational Mapping
Технология, которая преобразует Java-объекты в строки таблиц и обратно.

ORM — это механизм превращения объектов в таблицы.
JPA — это набор официальных Java-правил, по которым этот механизм должен работать.

<h2 id="hibernate-basics">12. Что такое Hibernate?</h2>

Что такое Hibernate

Hibernate — это самая популярная ORM-библиотека для Java и реализация стандарта JPA.
Она позволяет работать с базой данных через Java-объекты вместо SQL-кода.

Что делает Hibernate

сохраняет объекты в базу

загружает данные и превращает их в объекты

автоматически создаёт и обновляет таблицы

управляет связями между сущностями

кэширует данные

следит за состояниями объектов (new, managed, detached, removed)

Почему Hibernate удобен

меньше SQL

меньше ошибок

больше автоматизации

переносимость между разными базами

мощные возможности: HQL, Criteria API, ленивые загрузки, кэширование

<h2 id="jpa-vs-hibernate">13. В чем разница между JPA и Hibernate? Как связаны эти понятия?</h2>

Что такое Hibernate

Hibernate — это самая популярная ORM-библиотека для Java и реализация стандарта JPA.
Она позволяет работать с базой данных через Java-объекты вместо SQL-кода.

Что делает Hibernate

сохраняет объекты в базу

загружает данные и превращает их в объекты

автоматически создаёт и обновляет таблицы

управляет связями между сущностями

кэширует данные

следит за состояниями объектов (new, managed, detached, removed)

Почему Hibernate удобен

меньше SQL

меньше ошибок

больше автоматизации

переносимость между разными базами

мощные возможности: HQL, Criteria API, ленивые загрузки, кэширование
да
<h2 id="jpa-hibernate-interfaces">14. Какие классы/интерфейсы относятся к JPA/Hibernate?</h2>

JPA:

Интерфейсы EntityManager, EntityManagerFactory, EntityTransaction, Query, CriteriaBuilder и аннотации @Entity, @Table, @Id, @OneToMany и т.д.

Hibernate:

Нативные Session, SessionFactory, Configuration, Transaction, Query/Criteria, а также специфические классы Работа с кэшем, типами, конфигурацией и собственные исключения.​

<h2 id="hibernate-annotations">15. Основные аннотации Hibernate</h2>

@Entity — делает класс сущностью.
@Table — задаёт имя таблицы и параметры.
@Id — помечает поле как первичный ключ.
@GeneratedValue — указывает стратегию генерации ключей.
@Column — задаёт параметры столбца.
@Transient — поле не сохраняется в базу.
@OneToOne, @OneToMany, @ManyToOne, @ManyToMany — описывают связи между сущностями.
@JoinColumn — указывает внешний ключ.

<h2 id="hql-vs-sql">16. Отличия HQL от SQL</h2>

SQL работает с таблицами и колонками.
HQL работает с сущностями и их полями.
SQL зависит от конкретной базы данных.
HQL автоматически переводится Hibernate в SQL выбранной базы.
HQL поддерживает работу с объектами, связями и наследованием.

<h2 id="hibernate-query">17. Что такое Query и как передавать параметры</h2>

Query — объект для выполнения HQL или JPQL запросов.
Создаётся через session.createQuery() или entityManager.createQuery().
Параметры передаются через setParameter().
Поддерживаются именованные параметры (:name) и позиционные (1, 2, 3).
Основные методы: list(), getResultList(), uniqueResult(), getSingleResult().

<h2 id="hbm2ddl-options">18. Параметры hbm2ddl</h2>

none — ничего не делает с базой.
validate — проверяет соответствие структуры таблиц сущностям.
update — обновляет схему, добавляя недостающие столбцы.
create — создаёт таблицы заново, удаляя данные.
create-drop — создаёт таблицы при запуске и удаляет при завершении.

<h2 id="jpa-entity-requirements">19. Требования JPA к Entity-классам</h2>

Класс должен быть помечен @Entity.
Должен иметь публичный конструктор без параметров.
Должно быть поле @Id.
Класс не должен быть final.
Поля должны иметь геттеры и сеттеры.
Рекомендуется реализовать Serializable.

<h2 id="hibernate-entity-lifecycle">20. Жизненный цикл Entity в Hibernate</h2>

Transient — объект создан через new, не связан с Hibernate.
Persistent — объект находится под управлением сессии, изменения отслеживаются.
Detached — сессия закрыта, объект не отслеживается.
Removed — объект помечен на удаление и будет удалён после commit.



Как закрывается сonnection? 

В JDBC
connection.close();
Но хорошая практика — try-with-resources, чтобы соединение закрывалось автоматически даже при исключениях:

В Hibernate / JPA:

Соединение не закрываешь ты.

Оно управляется Session / EntityManager, а те — пулом соединений (DataSource, HikariCP).

Когда ты делаешь:

session.close();


или

entityManager.close();


то Hibernate возвращает connection в пул, а не убивает его.

Если коротко:
В JDBC — вручную через close().
В Hibernate — закрывается сессия, а connection возвращается в пул.

Statement
Каждый раз отправляет SQL как текст.
База компилирует запрос заново → нет кэширования плана.
Подходит только для простых запросов без параметров.

PreparedStatement
SQL компилируется один раз, база создаёт execution plan и кладёт его в кэш.
Дальнейшие вызовы используют готовый план, подставляя только параметры → быстрее и безопаснее.

Итог:
Statement — без кэша, медленнее. (Для DDL (CREATE TABLE, ALTER TABLE, DROP …) используется Statement, не PreparedStatement.

Коротко почему:

DDL обычно выполняется один раз (миграции, инициализация).

DDL не имеет параметров, которые можно было бы подставлять.

Смысл кэширования execution plan отсутствует — структура запроса меняется и используется однократно.

Поэтому в DDL → Statement.)
PreparedStatement — кэширует план, быстрее и защищает от SQL-инъекций.


Почему не использовать PreparedStatement всегда?

Если говорить строго — в нормальных приложениях его и используют почти всегда.
Но есть несколько редких случаев, когда Statement всё-таки проще:

Нет параметров, запрос вызывается один раз
Например, разовая команда типа
CREATE TABLE ...
Тут кэширование плана ничего не даёт.

Динамический SQL, где меняется сама структура запроса
Если запрос меняется каждый раз (а не только параметры), то PreparedStatement не поможет, потому что кэшировать нечего.

Чрезвычайно простой скриптовый сценарий
Типа быстрого теста или миграции, где нет смысла писать шаблон с ?.


Что пишется в pom.xml

Это рецепт сборки Maven-проекта. В нём обычно указывают:

project info — groupId, artifactId, version

dependencies — какие библиотеки нужны

plugins — как именно собирать проект и что запускать

build настройки — компилятор, директории, ресурсы

repository — откуда Maven скачивает артефакты

Кто такой сборщик проекта

Это Maven — инструмент, который:

скачивает зависимости

компилирует

запускает тесты

пакует проект в jar/war

управляет версиями, плагинами и фреймворком сборки

Фазы Maven Lifecycle (которыми можно управлять)

Главные фазы “default lifecycle”:

validate — проверка структуры проекта

compile — компиляция

test — тестирование

package — создание JAR/WAR

verify — проверки качества

install — кладёт артефакт в локальный репозиторий

deploy — выкладывает на удалённый репозиторий

Когда вызываешь фазу, Maven выполняет все предыдущие автоматически.

Как открыть lifecycle в IDE

В IntelliJ IDEA:
View → Tool Windows → Maven → Lifecycle

Там список фаз, которые можно запускать кликом.

Разница между dependency и plugin в pom.xml
Dependency (зависимость)

Ты подключаешь библиотеку, которая нужна твоему коду во время компиляции или рантайма.

Пример: Hibernate, MySQL driver, JUnit.

Они нужны приложению.

Plugin (плагин)

Плагин расширяет Maven и управляет процессом сборки.

Примеры:

maven-compiler-plugin — компилирует код

surefire-plugin — запускает тесты

shade-plugin — собирает “fat jar”

Они нужны Maven, а не твоему приложению.

Короткий итог (для шпаргалки)

pom.xml = описание проекта + зависимости + плагины + фазы сборки

dependency = библиотека для работы твоего кода

plugin = инструмент для Maven, влияющий на процесс сборки

lifecycle = цепочка фаз Maven от validate до deploy


